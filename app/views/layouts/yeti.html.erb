<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "pm_tool" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "yeti", :media => "all" %>

    <%= yield(:page_stylesheet) if content_for?(:page_stylesheet) %>
    <%= javascript_include_tag "yeti" %>
    <%= yield(:page_javascript) if content_for?(:page_javascript) %>

  </head>
  <body>

  <header>
    <div class="container"><h1><%= link_to 'PM Tool', root_path %></h1></div>
  </header>
  <main role="main">
    <div class="row">
      <div class="col-md-3">
        <ul class="nav nav-pills nav-stacked">
          <% @projects.each do |project| %>
            <li class="active"><%= link_to project.name, project %></li>
          <% end %>
          <li><a href="#">test</a></li>
        </ul>
      </div>
      <div class="col-md-9">
        <%= yield %>
      </div>
    </div>
  </main>

    <!-- Javascripts
    ================================================== -->
    <!-- Placed at the end of the document so the pages load faster -->

  </body>
</html>
